# Todo App

A simple CLI todo application (with planted bugs for testing the agent).

## Usage

```bash
python cli.py add "Buy milk" --priority high --tag shopping
python cli.py list
python cli.py done 1
python cli.py rm 1
python cli.py save
```
